# Animal Pairing and Grouping Tool™ — Legacy desktop

Legacy Python GUI lives at `main.py`. The actively maintained modern app is in `modern-app/` and documented in the root `README.md`.

Run legacy:
```bash
python main.py
```
Requires Python 3 with Tkinter, pandas, openpyxl.
